[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = [
  "cd backend",
  "npm install --production=false"
]

[phases.build]
cmds = [
  "cd backend",
  "npm install --production=false"
]

[start]
cmd = "cd backend && npm start"

